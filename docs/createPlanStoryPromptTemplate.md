# Create Story Plan{{CONTEXT_TITLE}}

> **Before proceeding, read the copilot instructions at `.github/copilot-instructions.md`.**  
> This file contains important project-specific rules and coding conventions that must be followed.

## Task

Add one or more **Story** work items to the plan file for the feature described in the **Story Specifications** section below.

**Required process:**
1. Fetch the latest context from Azure DevOps — read the parent epic and feature using the scripts in `src/`. Do NOT rely on memory or previous context from earlier conversations.
2. Refer to `example-hierarchy.md` for reference formatting.
3. Apply all rules from the sections below when writing the story/stories.
4. If a plan file already exists for this feature in `docs/plans/`, append to it. Otherwise create a new file.

## Azure DevOps Configuration

{{AZDO_CONFIG}}

## Story Specifications

> ⚠️ **UPDATE THIS SECTION** with the specific story/stories you want to add before sending this prompt.

- **Goal**: {Describe what this story should accomplish}
- **Scope**: {What is in scope — behaviour, scripts, fields, tests}
- **Special considerations**: {Technical constraints, patterns to follow, known issues, etc.}
- **Related links**: {Links to specs, examples, or other context if available}

## Plan Creation Rules

{{PLAN_CREATION_RULES}}

## Story Rules

{{STORY_RULES}}
{{EXTRA_STORY_RULES_SECTION}}
## Architectural Rules

{{ARCH_RULES}}

## Output Requirements

- If the feature already has a plan file in `docs/plans/`, append the new `### Story:` block(s) to that file.
- Otherwise create a new file named `docs/plans/plan-tbd-story{StoryTitle}.md` (camelCase title, no spaces) containing the epic/feature header and the new story/stories.
  The `tbd` placeholder is replaced with the story's work item ID once the plan is persisted via `NewAzDoHierarchyFromMarkdown.ps1`.
  Example: `plan-tbd-storyReadOnlyDateFields.md` → renamed to `plan-2750-storyReadOnlyDateFields.md` after creation.
- Follow `example-hierarchy.md` for structure and formatting.
- Mark each story with realistic story points following the estimation guidelines in the Story Rules section.
- Do NOT include a `{WorkItemId}` for new stories.
- **Every non-blank line in the output file MUST end with two trailing spaces** (`  `) — see the "Markdown line break rule" in the Plan Creation Rules above.

---

*Generated by `docs/{{GENERATED_BY}}` — {{TIMESTAMP}}*
